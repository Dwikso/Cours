#include <stdio.h>

int main(void) {
  int a, b;
  a = 16;
  b = 016;
  printf("Pourquoi la variables a (=%d) a une valeur différentes de la "
         "variables b (=%d) ? \n",
         a, b);
  return (0);
}
