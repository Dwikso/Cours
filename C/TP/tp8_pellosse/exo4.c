#include <stdio.h>
#include <stdlib.h>

#define k 3

int TicTacToe_gagnant(int t[][]) {
  int i,j:
  int premier = tab[0][0]
  for(i = 0; i <=3; i++) {
    for(j = 0; j <= 3; j++) {
      if (premier == tab[i][j]) {
        return 1;
      }
    }
  }
}
